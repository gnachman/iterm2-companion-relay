node_modules/
dist/
*.log
# Stray captured terminal transcript (leaks hostname / prompts); never commit
/output
# SQLite state (relay pairings); never commit
*.db
*.db-shm
*.db-wal
# Filled-in deploy config: holds the dashboard password / push token. Never commit.
ops/deploy.env
