{
  "name": "iterm2-companion-relay",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Self-hosted Node relay for iTerm2 Companion remote connectivity. Splices a phone and a Mac through outbound WebSockets; sees only ciphertext. See ../../docs/companion-relay-design.md.",
  "scripts": {
    "start": "node bin/relay.js",
    "test": "vitest run --config vitest.node.config.js",
    "test:watch": "vitest --config vitest.node.config.js"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.2.6",
    "vitest": "^3.2.4"
  },
  "dependencies": {
    "@peculiar/x509": "^2.0.0",
    "better-sqlite3": "^12.11.1",
    "reflect-metadata": "^0.2.2",
    "ws": "8.21.0"
  }
}
